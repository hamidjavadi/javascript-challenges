Write a function isPalindrome(str) that returns true if the string is a palindrome (same forwards and backwards), otherwise false

```javascript
isPalindrome("racecar"); // true
isPalindrome("hello"); // false
```
